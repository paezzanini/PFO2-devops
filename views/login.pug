doctype html
html
  head
    title Iniciar Sesión
    link(rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.1.3/css/bootstrap.min.css")
    script(src="/js/login.js") // Enlaza tu archivo JS
  body
    .d-flex.justify-content-center.align-items-center.min-vh-100
      .card.p-4.shadow-sm(style="max-width: 320px;")
        h1.text-center.mb-4 Iniciar sesión

        // Mostrar mensaje de error si existe
        if error
          .alert.alert-danger.mb-3
            | #{error}

        form(onsubmit="event.preventDefault(); login();")
          .mb-3
            label(for="username" class="form-label") Usuario:
            input#username.form-control.form-control-sm(type="text" placeholder="Usuario" required)
          .mb-3
            label(for="password" class="form-label") Contraseña:
            input#password.form-control.form-control-sm(type="password" placeholder="Contraseña" required)
          button(type="submit" class="btn btn-primary btn-sm w-100") Iniciar sesión
          p.text-center.mt-3
        | ¿No tienes una cuenta? 
        a(href='/register') Regístrate aquí

 

      